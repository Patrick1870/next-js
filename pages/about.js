import React from 'react';

// you dont need to use React Router to access /about. Every component is accesible trough pages, e.g. localhost:3000/about
const about = () => {
	return (
		<div>About!</div>
	)
};

export default about;